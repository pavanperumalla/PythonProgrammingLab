def print_arr(li):
	for i in li:
              print(i,end='')
elements=[]
limit=int(input("enter limits for lists: "))
for i in range(limit):
	val=int(input("enter the values: "))
	elements.append(val)
print_arr(elements)
